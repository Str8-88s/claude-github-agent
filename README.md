# claude-github-agent
A conversational AI agent that answers natural language questions about GitHub repositories using the Anthropic API with tool use. Ask questions in plain English. The agent decides which GitHub API calls to make, executes them, and returns a clear answer.
